const { expect } = require('chai')

describe('Helpers', function () {
  describe('isEmailValid', function () {
    it('should return false for an email that has an invalid domain', function () {})

    it('should return false for an email that is missing the @ symbol', function () {})

    it('should return true for an email that is valid', function () {})
  })

  describe('parseFormData', function () {
    it('should throw an error if no form data is passed', function () {})

    it('should return an empty array if an empty object is passed', function () {})

    it('should return an array of strings compose of the keys and values of an object', function () {})
  })
})